# 도메인 용어 사전 (Domain Glossary)

Piku 서비스의 도메인 용어를 정리한 문서입니다.

---

## 핵심 도메인

### Diary (일기)
사용자가 작성하는 일기. 텍스트 내용, 사진, 공개 범위를 포함.

| 필드 | 설명 |
|------|------|
| `content` | 일기 본문 (최대 500자) |
| `status` | 공개 범위 (PUBLIC, FRIENDS, PRIVATE) |
| `date` | 일기 작성 날짜 |

### Photo (사진)
일기에 첨부되는 사진. AI 생성 또는 사용자 업로드.

| 필드 | 설명 |
|------|------|
| `url` | 저장소 경로 |
| `represent` | 대표 사진 여부 |

### User (사용자)
서비스 이용자.

| 필드 | 설명 |
|------|------|
| `email` | 로그인 이메일 |
| `nickname` | 표시 이름 |
| `avatar` | 프로필 사진 경로 |

---

## 소셜 도메인

### Friend (친구)
양방향 친구 관계.

### FriendRequest (친구 요청)
친구 신청 및 수락 대기 상태.

| 상태 | 설명 |
|------|------|
| `PENDING` | 요청 대기 |
| `ACCEPTED` | 수락됨 (친구 관계 성립) |
| `REJECTED` | 거절됨 |

### FriendStatus
두 사용자 간의 현재 관계 상태.

```
NONE → PENDING → FRIEND
```

---

## 피드 도메인

### Feed (피드)
사용자에게 보여지는 일기 목록. 우선순위: 친구 일기 → 공개 일기 → 최근 클릭 일기.

### FeedClick (피드 클릭)
사용자의 일기 클릭 기록. 읽음 처리 및 추천에 활용.

| 필드 | 설명 |
|------|------|
| `clickedAt` | 클릭 시각 |
| `viewDurationSeconds` | 조회 시간 (초) |

---

## 참여 도메인

### Like (좋아요)
일기에 대한 좋아요. 사용자당 일기당 1회.

### Comment (댓글)
일기에 대한 댓글. 답글(Reply) 지원.

### Notification (알림)
사용자에게 전송되는 알림.

| 타입 | 설명 |
|------|------|
| `FRIEND_REQUEST` | 친구 요청 알림 |
| `FRIEND_ACCEPT` | 친구 수락 알림 |
| `COMMENT` | 댓글 알림 |
| `REPLY` | 답글 알림 |
| `FRIEND_DIARY` | 친구 새 일기 알림 |
| `LIKE` | 좋아요 알림 |

---

## 추천 도메인 (예정)

### DiaryMetadata (일기 메타데이터)
일기의 토픽 분석 결과 저장.

| 필드 | 설명 |
|------|------|
| `primaryTopic` | 주요 토픽 (travel, food, work 등) |
| `topics` | 토픽별 점수 (JSON) |
| `qualityScore` | 콘텐츠 품질 점수 (0.0~1.0) |

### UserPreference (사용자 선호도)
사용자의 토픽 선호도 프로필.

| 필드 | 설명 |
|------|------|
| `topicAffinities` | 토픽별 선호도 (JSON) |

---

## 공개 범위 (Status)

| 값 | 설명 |
|------|------|
| `PUBLIC` | 모든 사용자에게 공개 |
| `FRIENDS` | 친구에게만 공개 |
| `PRIVATE` | 본인에게만 공개 |

---

## 토픽 분류

| 토픽 | 설명 | 키워드 예시 |
|------|------|-------------|
| `travel` | 여행 | 여행, 비행기, 호텔, 관광 |
| `food` | 음식 | 맛집, 카페, 요리 |
| `fitness` | 운동/건강 | 헬스, 러닝, 등산 |
| `work` | 업무 | 회사, 프로젝트, 회의 |
| `family` | 가족 | 부모님, 엄마, 아빠 |
| `friends` | 친구 | 모임, 약속 |
| `hobby` | 취미 | 게임, 영화, 책 |
| `nature` | 자연 | 바다, 산, 공원 |
| `daily` | 일상 | 오늘, 하루, 일상 |
| `reflection` | 회고/감정 | 생각, 느낌, 감사 |
